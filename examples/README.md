# Companion Memory example

与 `skill-forge/built/companion-memory/companion-memory` 同构。

```bash
python skill-core/skill.py install examples/companion-memory/companion-memory --host cursor --scope project
```

重新导出：`python skill-core/scripts/export_examples.py`（需完整 skill-forge/built 树）
