# MD 兼容视图 — `memory-gitignore.txt`

> **权威源文件**：同目录 `memory-gitignore.txt`（本文件由 `skill-core` 自动生成，请勿手改；改源文件后重新 `batch build` 或运行 compat 同步。）

| 项 | 值 |
|----|-----|
| 类型 | `txt` |
| 路径 | `refs/templates/memory-gitignore.txt` |
| 用途 | 非 Markdown 源文件。 |

## 内容

```text
# Copy to project memory/.gitignore
episodes/*.yaml
inbox/*.yaml
core/working.yaml
archive/
diary/
reflections/
```
