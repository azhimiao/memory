# MD 兼容视图 — `memory-index.yaml`

> **权威源文件**：同目录 `memory-index.yaml`（本文件由 `skill-core` 自动生成，请勿手改；改源文件后重新 `batch build` 或运行 compat 同步。）

| 项 | 值 |
|----|-----|
| 类型 | `yaml` |
| 路径 | `refs/templates/memory-index.yaml` |
| 用途 | YAML 配置或结构化数据。 |

## 内容

```yaml
version: "1.0"
updated_at: ""
wings:
  user: { rooms: [identity, preferences, boundaries] }
  relationship: { rooms: [milestones, rituals, nicknames] }
  ai-persona: { rooms: [character, voice-style] }
episode_count: 0
```
